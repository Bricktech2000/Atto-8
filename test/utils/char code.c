#include <ctype.h>
#include <stdio.h>

void main(void) {
  while (1) {
    // printf(" %u", getchar());
    printf(" @%x", getchar());
    // putchar(tolower(getchar()));
    // putchar(toupper(getchar()));
  }
}
